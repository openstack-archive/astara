AKANDA_RUG_DIR=$DEST/akanda-rug

for svc in q-l3 q-fwaas q-lbaas q-meta q-vpn; do
    if is_service_enabled $svc; then
        echo "Disabling $svc in favor of Akanda."
        disable_service $svc
    fi
done
